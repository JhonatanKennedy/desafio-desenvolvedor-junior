Para rodar essa aplicação é preciso ter uma imagem do mongodb rodando na sua máquina,
depois é preciso baixar as dependencias do backend com npm install,
após isso é preciso rodar o backend com npm start, depois podemos instalar as dependencias
do front end com npm install e por fim rodar o front com npm run.
